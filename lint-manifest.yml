platform: linux

image_resource:
  type: docker-image
  source:
    repository: 18fgsa/concourse-task

inputs:
- name: nessus-manager-config
- name: merged-manifest

run:
  path: bosh-lint
  args:
  - lint-manifest
  - --config
  - nessus-manager-config/bosh-lint.yml
  - merged-manifest/manifest.yml
